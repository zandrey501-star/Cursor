# 🔍 Мониторинг конкурентов - AI Ассистент

MVP приложение для анализа конкурентной среды с поддержкой мультимодальности (текст и изображения).

![Python](https://img.shields.io/badge/Python-3.9+-blue.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green.svg)
![OpenAI](https://img.shields.io/badge/OpenAI-GPT--4o-purple.svg)
![Selenium](https://img.shields.io/badge/Selenium-4.15+-orange.svg)

## 📋 Описание

Приложение позволяет:
- **Анализировать текст конкурентов** — получать структурированную аналитику с сильными/слабыми сторонами, уникальными предложениями и рекомендациями
- **Анализировать изображения** — баннеры, скриншоты сайтов, упаковки товаров с оценкой визуального стиля
- **Парсить сайты** — автоматически извлекать контент через Selenium, делать скриншоты и анализировать визуальный дизайн
- **Хранить историю** — последние 10 запросов сохраняются для быстрого доступа
- **Сохранять результаты** — все анализы автоматически сохраняются в папку `analise_history` в формате JSON

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Перейдите в папку проекта
cd competitor-monitor

# Создайте виртуальное окружение (опционально, но рекомендуется)
python -m venv venv

# Активируйте окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# Скопируйте пример
cp env.example.txt .env
```

Затем отредактируйте `.env`:

```env
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o-mini
```

### 3. Установка Chrome (для парсинга сайтов)

Для работы парсинга сайтов требуется Google Chrome:

- **macOS**: Chrome устанавливается автоматически через webdriver-manager
- **Linux**: `sudo apt-get install google-chrome-stable` (или эквивалент)
- **Windows**: Скачайте и установите Chrome вручную

### 4. Запуск приложения

```bash
# Запуск сервера
python run.py
```

Или альтернативный способ:

```bash
python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

Приложение будет доступно по адресу: **http://localhost:8000**

## 📁 Структура проекта

```
competitor-monitor/
├── backend/
│   ├── __init__.py
│   ├── main.py              # FastAPI приложение
│   ├── config.py            # Конфигурация
│   ├── models/
│   │   ├── __init__.py
│   │   └── schemas.py       # Pydantic модели
│   └── services/
│       ├── __init__.py
│       ├── openai_service.py    # Работа с OpenAI API
│       ├── parser_service.py    # Парсинг через Selenium
│       └── history_service.py   # Управление историей и сохранение результатов
├── frontend/
│   ├── index.html           # HTML страница
│   ├── styles.css           # Стили
│   └── app.js               # JavaScript логика
├── analise_history/         # Папка с сохраненными результатами анализов
│   ├── text_*.json          # Результаты анализа текста
│   ├── picture_*.json       # Результаты анализа изображений
│   └── site_*.json          # Результаты парсинга сайтов
├── requirements.txt         # Зависимости Python
├── run.py                   # Скрипт запуска
├── history.json             # История последних 10 запросов
├── env.example.txt          # Пример файла переменных окружения
├── README.md                # Этот файл
└── docs.md                  # Документация API
```

## 🔧 Функциональность

### Анализ текста (`POST /analyze_text`)
- Принимает текст конкурента (минимум 10 символов)
- Возвращает структурированную аналитику:
  - Сильные стороны
  - Слабые стороны
  - Уникальные предложения
  - Рекомендации по улучшению
  - Общее резюме
- **Автоматически сохраняет** результат в `analise_history/text_YYYYMMDD_HHMMSS.json`

### Анализ изображений (`POST /analyze_image`)
- Принимает изображения: PNG, JPG, GIF, WEBP
- Возвращает:
  - Описание изображения
  - Маркетинговые инсайты
  - Оценку визуального стиля (0-10)
  - Анализ визуального стиля
  - Рекомендации
- **Автоматически сохраняет** результат в `analise_history/picture_YYYYMMDD_HHMMSS.json`

### Парсинг сайтов (`POST /parse_demo`)
- Принимает URL сайта
- Использует **Selenium** для:
  - Открытия страницы в Chrome (headless режим)
  - Создания скриншота страницы
  - Извлечения: title, h1, первый абзац
- Передаёт скриншот и текст в AI для **комплексного анализа** (визуальный дизайн + контент)
- **Автоматически сохраняет** результат в `analise_history/site_<название_сайта>_YYYYMMDD_HHMMSS.json`

### История (`GET /history`)
- Хранит последние 10 запросов
- Сохраняет тип запроса, краткое описание, время
- Доступна через API и веб-интерфейс

### Очистка истории (`DELETE /history`)
- Очищает историю запросов

## 💾 Сохранение результатов

Все результаты анализов автоматически сохраняются в папку `analise_history/`:

- **Формат**: JSON
- **Именование файлов**:
  - `text_YYYYMMDD_HHMMSS.json` — анализ текста
  - `picture_YYYYMMDD_HHMMSS.json` — анализ изображения
  - `site_<название_сайта>_YYYYMMDD_HHMMSS.json` — парсинг сайта

Каждый файл содержит:
- Тип анализа
- Временную метку
- Входные данные
- Полные результаты анализа

## 🛠️ Технологии

- **Backend**: FastAPI, Python 3.9+
- **AI**: OpenAI GPT-4o-mini (или GPT-4.1)
- **Frontend**: Vanilla JS, CSS3
- **Парсинг**: Selenium 4.15+, Chrome WebDriver
- **Валидация**: Pydantic
- **Управление зависимостями**: webdriver-manager (автоматическая установка ChromeDriver)

## 📖 API Документация

После запуска сервера доступна интерактивная документация:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **Health Check**: http://localhost:8000/health

Подробная документация API в файле [docs.md](docs.md)

## ⚠️ Требования

- **Python 3.9+** (рекомендуется 3.13+)
- **OpenAI API ключ** с доступом к GPT-4o-mini или GPT-4.1
- **Google Chrome** браузер (для парсинга сайтов)
- **Интернет-соединение** для работы AI и парсинга
- **ChromeDriver** устанавливается автоматически через webdriver-manager

## 🔍 Особенности парсинга

Парсинг сайтов использует Selenium для:
- ✅ Загрузки динамического контента (JavaScript)
- ✅ Создания скриншотов страниц
- ✅ Мультимодального анализа (текст + визуальный дизайн)
- ✅ Обработки сложных SPA приложений
- ✅ Автоматической настройки ChromeDriver

## 🐛 Устранение неполадок

### Ошибка при парсинге сайта

1. Убедитесь, что Chrome установлен:
   ```bash
   # macOS
   which google-chrome
   
   # Linux
   google-chrome --version
   ```

2. ChromeDriver устанавливается автоматически, но можно проверить:
   ```bash
   pip install --upgrade webdriver-manager
   ```

3. Проверьте логи в терминале для деталей ошибки

### Ошибка подключения к OpenAI

1. Проверьте, что `OPENAI_API_KEY` установлен в `.env`
2. Убедитесь, что ключ действителен и имеет доступ к нужным моделям
3. Проверьте баланс API аккаунта

### Порт 8000 занят

Измените порт в `.env` или в `backend/config.py`:
```python
api_port = 8001  # или другой свободный порт
```

## 📝 Лицензия

MIT License
