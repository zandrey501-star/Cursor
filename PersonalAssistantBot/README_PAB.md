# Personal Assistant Telegram Bot

Мультимодальный Telegram-бот-ассистент на Python с использованием OpenAI API и LangChain.

## Структура проекта

```
PersonalAssistantBot/
├── .env.example              # Шаблон переменных окружения
├── requirements.txt          # Зависимости проекта
├── main.py                   # Точка входа приложения
├── config.py                 # Управление конфигурацией
├── bot/                      # Директория логики бота
│   ├── __init__.py           # Инициализация пакета
│   ├── handlers.py           # Обработчики команд и сообщений
│   ├── conversation.py       # Управление историей диалога
│   └── file_tracker.py       # Отслеживание загруженных файлов
├── llm/                      # Модуль интеграции с OpenAI
│   ├── __init__.py           # Инициализация пакета
│   ├── client.py             # Асинхронный клиент OpenAI
│   ├── chat.py               # Генерация текстовых ответов (GPT-4o)
│   ├── vision.py             # Анализ изображений (GPT-4V)
│   └── image_generation.py   # Генерация изображений (DALL·E 3)
├── rag/                      # Модуль RAG (Retrieval-Augmented Generation)
│   ├── __init__.py           # Инициализация пакета
│   ├── vector_store.py       # Векторное хранилище ChromaDB
│   ├── ingest.py             # Загрузка и обработка документов
│   └── chain.py              # RAG цепочки для генерации ответов
├── multimodal/               # Модуль мультимодальности (STT/TTS)
│   ├── __init__.py           # Инициализация пакета
│   ├── stt.py                # Speech-to-Text (распознавание речи)
│   └── tts.py                # Text-to-Speech (синтез речи)
├── chroma_db/                # Персистентное хранилище ChromaDB (создается автоматически)
├── user_documents/           # Исходные загруженные файлы пользователей (создается автоматически)
│   └── {user_id}/            # Папка для каждого пользователя
├── user_files.json           # Метаданные загруженных файлов (создается автоматически)
└── README.md                 # Этот файл
```

## Установка

1. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Заполните `.env` файл своими ключами:
- `OPENAI_API_KEY` - ваш API ключ OpenAI
- `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота

## Запуск

1. Убедитесь, что у вас создан файл `.env` с необходимыми переменными:
```bash
cp .env.example .env
# Отредактируйте .env и добавьте свои ключи
```

2. Запустите бота:
```bash
python main.py
# или
python3 main.py
```

После запуска вы увидите в логах:
- Информацию о регистрации всех обработчиков
- Подтверждение подключения к Telegram API
- Информацию о боте (имя, username, ID)

Бот будет отвечать на команды:
- `/start` - приветствие, инициализация диалога, показ меню
- `/help` - справка по использованию
- `/new_chat` - начать новый диалог (очистить историю)
- `/reset_chat` - сбросить диалог с подтверждением
- `/generate_image` - сгенерировать изображение с помощью DALL·E 3
- `/upload_file` - загрузить документ в базу знаний
- `/list_files` - список загруженных файлов
- `/delete_file` - удалить файл из базы знаний

**Примечание:** Для остановки бота нажмите `Ctrl+C` в терминале.

## Документация

### Основные ресурсы

- **Telegram Bot API**: https://core.telegram.org/bots/api
- **OpenAI Python Library**: https://github.com/openai/openai-python
- **python-telegram-bot**: https://docs.python-telegram-bot.org/ (рекомендуемая библиотека)

### Полезные ссылки

- **LangChain Documentation**: https://python.langchain.com/
- **LangChain OpenAI Integration**: https://python.langchain.com/docs/integrations/llms/openai
- **ChromaDB Documentation**: https://docs.trychroma.com/
- **LangChain ChromaDB Integration**: https://python.langchain.com/docs/integrations/vectorstores/chroma

## Функциональность

### ✅ Реализовано и протестировано

- ✅ **Структура проекта** - модульная архитектура
- ✅ **Команды бота**:
  - `/start` - приветствие, инициализация диалога, показ меню
  - `/help` - справка по использованию бота
  - `/new_chat` - начало нового диалога (очистка истории)
  - `/reset_chat` - подтверждение и выполнение сброса диалога
  - `/generate_image` - генерация изображений с помощью DALL·E 3
  - `/upload_file` - загрузка документов в базу знаний
  - `/list_files` - просмотр списка загруженных файлов
  - `/delete_file` - удаление файла из базы знаний
- ✅ **Клавиатура меню** (ReplyKeyboardMarkup) - быстрый доступ к командам
- ✅ **Управление историей диалогов** (ConversationManager):
  - Инициализация диалога для каждого пользователя
  - Добавление сообщений в историю
  - Получение истории диалога в формате OpenAI
  - Сброс истории для отдельного пользователя
  - Ограничение длины истории (max_history=50)
- ✅ **Интеграция с OpenAI API**:
  - Асинхронный клиент OpenAI
  - Генерация текстовых ответов через GPT-4o
  - Анализ изображений через GPT-4V (gpt-4o vision)
  - Генерация изображений через DALL·E 3
- ✅ **Обработка текстовых сообщений**:
  - Интеграция с GPT-4o для генерации ответов
  - Умное автоматическое определение необходимости использования RAG
  - Распознавание RAG-запросов по ключевым словам и контексту:
    - Упоминание документов/базы знаний ("используя загруженные документы", "в файле", "на основе документа")
    - Упоминание конкретных файлов в кавычках ("используя 'имя файла.docx'")
    - Упоминание имени файла без кавычек (автоматическое распознавание по совпадению имени)
    - Комбинация слова "используя" с упоминанием файлов/документов/презентаций
    - Распознавание ключевых слов: "файл", "документ", "презентация", "загружен"
  - Использование RAG при наличии загруженных документов у пользователя
  - Сохранение контекста диалога
  - Системные сообщения для настройки поведения бота
  - Распознавание запросов на генерацию изображений
- ✅ **Обработка изображений**:
  - Анализ загруженных изображений через GPT-4V
  - Поддержка опциональных подписей (caption) как промптов
  - Детальное описание содержимого изображений
- ✅ **Генерация изображений**:
  - Создание изображений через DALL·E 3
  - Два режима работы: `/generate_image <описание>` и `/generate_image` с последующим описанием
  - Надежная загрузка изображений с retry логикой и fallback на aiohttp
  - Обработка таймаутов с предоставлением прямых ссылок
- ✅ **Обработка документов (RAG)**:
  - Загрузка документов (PDF, TXT, DOCX) в векторное хранилище
  - Сохранение исходных файлов в `user_documents/{user_id}/` для постоянного хранения
  - Разбиение документов на чанки для оптимального поиска
  - Хранение метаданных файлов (user_id как строка, source_file, source_path)
  - Фильтрация документов по пользователю с единообразным хранением типов
  - Удаление документов из базы знаний вместе с исходными файлами
- ✅ **RAG (Retrieval-Augmented Generation)**:
  - Векторное хранилище на базе ChromaDB с персистентным хранением
  - Embeddings через `text-embedding-3-small`
  - Автоматическое извлечение топ-10 релевантных фрагментов документов (улучшенная выборка контекста)
  - Генерация подробных ответов на основе контекста из документов с использованием GPT-4o
  - Улучшенные промпты для детальных кратких содержаний и структурированных ответов
  - Использование max_tokens из конфигурации для более длинных ответов
  - Интеграция с LangChain 1.x (LCEL - LangChain Expression Language)
  - Проверка наличия документов перед выполнением RAG-запросов
  - Детальное логирование процесса определения и выполнения RAG-запросов
  - Логирование длины контекста и предупреждения при коротких ответах
- ✅ **Управление файлами**:
  - Отслеживание загруженных файлов пользователей
  - Сохранение исходных файлов в `user_documents/{user_id}/` для постоянного доступа
  - Метаданные файлов в JSON формате (`user_files.json`)
  - Удаление файлов из векторного хранилища и исходных файлов одновременно
- ✅ **Загрузка конфигурации** из `.env` файла
- ✅ **Обработка ошибок** с логированием и уведомлениями пользователя:
  - Обработка rate limits
  - Обработка таймаутов
  - Обработка ошибок аутентификации
  - Обработка ошибок контент-политики
- ✅ **Подключение к Telegram API** с проверкой соединения
- ✅ **Логирование** всех операций бота
- ✅ **Graceful shutdown** при остановке бота
- ✅ **Speech-to-Text (STT)** - распознавание голосовых сообщений:
  - Конвертация OGG (Telegram) → WAV через ffmpeg
  - Транскрибация через OpenAI Whisper API
  - Автоматическая обработка распознанного текста как обычного сообщения
- ✅ **Text-to-Speech (TTS)** - озвучивание ответов бота:
  - Генерация голосовых сообщений через OpenAI TTS API
  - Голос по умолчанию: "onyx"
  - Inline-кнопка "Озвучить ответ" для каждого текстового ответа
  - Поддержка формата MP3

### ⏳ В разработке

- ⏳ Персистентное хранение истории диалогов (база данных)
- ⏳ Расширенные возможности RAG (векторный поиск с фильтрами)
- ⏳ Поддержка дополнительных форматов документов

## Команды бота

- `/start` - Начать работу с ботом, показать приветствие и меню
- `/help` - Показать справку по использованию бота
- `/new_chat` - Начать новый диалог (очистить историю текущего сеанса)
- `/reset_chat` - Подтвердить и выполнить сброс диалога
- `/generate_image` - Сгенерировать изображение с помощью DALL·E 3
  - Использование: `/generate_image <описание>` - генерация сразу
  - Или: `/generate_image` (затем отправьте описание отдельным сообщением)
- `/upload_file` - Загрузить документ в базу знаний
  - Поддерживаемые форматы: PDF (.pdf), TXT (.txt), DOCX (.docx)
  - Отправьте команду, затем файл
- `/list_files` - Показать список всех загруженных файлов
- `/delete_file` - Удалить файл из базы знаний
  - Использование: `/delete_file <имя файла>` или интерактивный выбор

## Возможности бота

- 💬 **Текстовый чат** - общение с AI-ассистентом на базе GPT-4o с сохранением контекста
- 🖼️ **Анализ изображений** - детальный анализ загруженных изображений через GPT-4V
- 🎨 **Генерация изображений** - создание изображений по описанию через DALL·E 3
- 📚 **База знаний (RAG)** - загрузка документов и вопросы по их содержимому
  - Автоматическое использование RAG при упоминании документов
  - Векторный поиск по содержимому документов
  - Ответы на основе контекста из загруженных файлов
  - **Использование**: 
    - Загрузите файл через `/upload_file`
    - Затем задайте вопрос, используя одну из фраз:
      - "используя загруженные документы" / "в загруженных файлах"
      - "используя файл 'имя файла.docx'" (с указанием конкретного файла в кавычках)
      - "используя презентация HUAWEI 3 рус.pdf" (можно и без кавычек - система распознает имя файла)
      - "на основе документа" / "из файла" / "по презентации"
      - Или просто "используя 'имя файла.docx'" - система автоматически распознает намерение по ключевым словам и упоминанию файла
- 🎤 **Голосовые сообщения (STT)** - распознавание речи:
  - Отправьте голосовое сообщение в бот
  - Автоматическое распознавание через Whisper API
  - Обработка распознанного текста как обычного сообщения
  - Поддержка русского языка
- 🔊 **Озвучивание ответов (TTS)** - синтез речи:
  - Inline-кнопка "🔊 Озвучить ответ" под каждым ответом бота
  - Генерация голосовых сообщений через OpenAI TTS
  - Голос по умолчанию: "onyx" (мужской, глубокий)
  - Формат: MP3
- 📝 **История диалога** - автоматическое сохранение контекста разговора

## Технические детали

### Архитектура

Проект структурирован модульно с четким разделением ответственности:

- **`main.py`** - Точка входа, инициализация и запуск бота:
  - Загрузка конфигурации
  - Создание Application с настройками таймаутов
  - Регистрация всех обработчиков
  - Запуск polling
  - Обработка ошибок и graceful shutdown

- **`config.py`** - Управление конфигурацией:
  - Загрузка переменных окружения из `.env`
  - Валидация обязательных параметров (токен, API ключ)
  - Предоставление функций для доступа к настройкам

- **`bot/handlers.py`** - Обработчики команд и сообщений:
  - Обработчики команд (`start_command`, `help_command`, `new_chat_command`, `reset_chat_command`, `generate_image_command`, `upload_file_command`, `list_files_command`, `delete_file_command`)
  - Обработчик текстовых сообщений (`handle_message`) с интеграцией GPT-4o и RAG
  - Обработчик изображений (`handle_photo`) с интеграцией GPT-4V
  - Обработчик документов (`handle_document`) с интеграцией RAG
  - Обработчик голосовых сообщений (`handle_voice`) с интеграцией STT
  - Обработчик callback-запросов для TTS (`handle_tts_callback`)
  - Функция создания клавиатуры меню с inline-кнопками
  - Вспомогательные функции для генерации изображений и работы с файлами

- **`bot/conversation.py`** - Управление историей диалогов:
  - Класс `ConversationManager` для хранения истории каждого пользователя
  - Методы для добавления, получения и сброса истории
  - Метод `get_messages()` для получения истории в формате OpenAI
  - Ограничение длины истории для оптимизации

- **`bot/file_tracker.py`** - Отслеживание загруженных файлов:
  - Хранение метаданных файлов в JSON формате
  - Функции для добавления, получения и удаления файлов
  - Связь файлов с пользователями (user_id)

- **`llm/client.py`** - Инициализация асинхронного клиента OpenAI:
  - Singleton паттерн для переиспользования клиента
  - Автоматическая инициализация при первом использовании

- **`llm/chat.py`** - Генерация текстовых ответов:
  - Функция `generate_response()` для работы с GPT-4o
  - Поддержка системных сообщений, сообщений пользователя и ассистента
  - Валидация формата сообщений

- **`llm/vision.py`** - Анализ изображений:
  - Функция `analyze_image()` для работы с GPT-4V
  - Поддержка опциональных текстовых промптов
  - Конвертация изображений в base64 для API

- **`llm/image_generation.py`** - Генерация изображений:
  - Функция `generate_image()` для работы с DALL·E 3
  - Надежная загрузка изображений с retry логикой
  - Fallback на aiohttp при проблемах с httpx
  - Экспоненциальная задержка между попытками

- **`rag/vector_store.py`** - Векторное хранилище:
  - Инициализация ChromaDB с персистентным хранилищем
  - Использование OpenAI embeddings (`text-embedding-3-small`)
  - Singleton паттерн для переиспользования экземпляра

- **`rag/ingest.py`** - Загрузка и обработка документов:
  - Функция `ingest_document()` для обработки файлов (PDF, TXT, DOCX)
  - Разбиение документов на чанки с помощью `RecursiveCharacterTextSplitter`
  - Добавление метаданных (user_id как строка, source_file, source_path)
  - Единообразное хранение user_id в строковом формате для корректной фильтрации
  - Функции для удаления документов из векторного хранилища с фильтрацией по user_id и source_file
  - Автоматическое удаление исходных файлов из `user_documents/` при удалении из базы знаний

- **`rag/chain.py`** - RAG цепочки:
  - Функция `get_rag_chain(user_id)` для создания RAG-цепей через LCEL с фильтрацией по пользователю
  - Функция `query_rag_chain(question, user_id)` для запросов к базе знаний
  - Извлечение топ-10 релевантных чанков с учетом фильтрации по user_id (улучшенная выборка)
  - Улучшенные промпты для генерации подробных кратких содержаний и структурированных ответов
  - Использование max_tokens из конфигурации для более длинных ответов
  - Проверка наличия документов пользователя перед выполнением запроса
  - Интеграция с GPT-4o для генерации детальных ответов на русском языке
  - Детальное логирование процесса RAG-запросов, включая длину контекста и ответа

- **`multimodal/stt.py`** - Speech-to-Text (распознавание речи):
  - Функция `transcribe_voice_message()` для конвертации голосовых сообщений в текст
  - Конвертация OGG (Telegram) → WAV через ffmpeg (прямой вызов через subprocess)
  - Транскрибация через OpenAI Whisper API (модель `whisper-1`)
  - Автоматическое определение языка
  - Поддержка Python 3.13 (использование ffmpeg напрямую вместо pydub)

- **`multimodal/tts.py`** - Text-to-Speech (синтез речи):
  - Функция `text_to_speech()` для конвертации текста в речь
  - Генерация голосовых сообщений через OpenAI TTS API (модель `tts-1`)
  - Поддержка голосов: "alloy", "echo", "fable", "onyx", "nova", "shimmer"
  - Голос по умолчанию: "onyx"
  - Формат вывода: MP3

### Особенности реализации

- **Обработка ошибок**: Глобальный обработчик ошибок с фильтрацией незначительных ошибок и детальной обработкой ошибок API
- **Логирование**: Подробное логирование всех операций для отладки
- **Таймауты**: Увеличенные таймауты для стабильной работы:
  - Telegram API: 30 секунд
  - Загрузка изображений: 120 секунд (read timeout)
- **Retry логика**: Автоматические повторные попытки при сбоях:
  - Генерация изображений: до 5 попыток с экспоненциальной задержкой
  - Fallback на aiohttp при проблемах с httpx
- **Валидация**: Проверка подключения к Telegram API при запуске
- **Модульность**: Четкое разделение логики по модулям
- **Асинхронность**: Полностью асинхронная архитектура для оптимальной производительности
- **RAG система**: Векторное хранилище на базе ChromaDB с персистентным хранением в `./chroma_db`
- **Умная маршрутизация**: Автоматическое определение использования RAG или обычного чата на основе контекста сообщения:
  - Анализ ключевых слов и фраз, указывающих на необходимость использования RAG
  - Распознавание упоминаний конкретных файлов в кавычках и без кавычек (автоматическое сопоставление имен)
  - Расширенный набор ключевых слов: "файл", "документ", "презентация", "загружен", "используя"
  - Проверка наличия загруженных документов у пользователя
  - Детальное логирование процесса принятия решений с указанием всех факторов обнаружения
- **Единообразие типов данных**: user_id хранится как строка во всех компонентах системы для корректной фильтрации в ChromaDB

### Переменные окружения

Файл `.env` должен содержать:

```env
OPENAI_API_KEY=your_openai_api_key_here
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
```

Опциональные настройки:
- `OPENAI_MODEL` - базовая модель для текстовых ответов (по умолчанию: `gpt-4o`)
- `OPENAI_VISION_MODEL` - модель для анализа изображений (по умолчанию: `gpt-4o`)
- `OPENAI_IMAGE_MODEL` - модель для генерации изображений (по умолчанию: `dall-e-3`)
- `OPENAI_TEMPERATURE` - температура модели (по умолчанию: `0.7`)
- `OPENAI_MAX_TOKENS` - максимальное количество токенов (по умолчанию: `2000`)
- `OPENAI_IMAGE_SIZE` - размер генерируемых изображений (по умолчанию: `1024x1024`)
- `OPENAI_IMAGE_QUALITY` - качество изображений для DALL·E 3: `standard` или `hd` (по умолчанию: `standard`)
- `MAX_CONVERSATION_HISTORY` - максимальная длина истории (по умолчанию: `50`)
- `ENABLE_IMAGE_GENERATION` - включить генерацию изображений (по умолчанию: `true`)

## Требования к системе

Для работы голосовых сообщений (STT) требуется установленный `ffmpeg`:

- **macOS**: `brew install ffmpeg`
- **Ubuntu/Debian**: `sudo apt-get install ffmpeg`
- **Windows**: скачать с https://ffmpeg.org/

`ffmpeg` используется для конвертации OGG файлов (формат Telegram) в WAV перед отправкой в Whisper API.

## Разработка

### Структура кода

Проект использует модульную архитектуру. Файлы с еще не реализованным функционалом содержат TODO комментарии на английском языке, описывающие что должна делать каждая функция.

### Основные модули

- `main.py` - Инициализация и запуск бота, регистрация обработчиков
- `config.py` - Управление конфигурацией и переменными окружения
- `bot/handlers.py` - Обработчики команд и сообщений
- `bot/conversation.py` - Управление историей диалогов пользователей
- `bot/file_tracker.py` - Отслеживание загруженных файлов пользователей
- `bot/__init__.py` - Экспорт основных функций и классов
- `llm/client.py` - Инициализация асинхронного клиента OpenAI
- `llm/chat.py` - Генерация текстовых ответов через GPT-4o
- `llm/vision.py` - Анализ изображений через GPT-4V
- `llm/image_generation.py` - Генерация изображений через DALL·E 3
- `llm/__init__.py` - Экспорт функций модуля llm
- `rag/vector_store.py` - Инициализация векторного хранилища ChromaDB
- `rag/ingest.py` - Загрузка и обработка документов для RAG
- `rag/chain.py` - Создание RAG цепочек для генерации ответов
- `rag/__init__.py` - Экспорт функций модуля RAG
- `multimodal/stt.py` - Speech-to-Text (распознавание голосовых сообщений)
- `multimodal/tts.py` - Text-to-Speech (озвучивание ответов)
- `multimodal/__init__.py` - Экспорт функций модуля multimodal

### Следующие шаги разработки

1. Добавить поддержку голосовых сообщений
2. Реализовать персистентное хранение истории диалогов (база данных)
3. Расширить возможности RAG (векторный поиск с более сложными фильтрами)
4. Добавить поддержку дополнительных форматов документов
5. Реализовать индексацию больших документов с оптимизацией

## Лицензия

MIT

